//Day 3 리뷰
//
//1. 자바 프로그램 시작 → 자바 파일 생성(클래스) → 시작점인 main 함수를 만들어야 함
//2. 코드가 많아지면 함수를 이용해서 쪼갠다. 변수가 많아지면 쪼갠다 → 클래스
//3. 클래스에 비슷한 성격의 데이터와 함수를 묶는다.
//4. main에서 불러서 사용
//    1) class를 직접 사용하는 방법 (static)
//    2) class의 복사본인 객체를 사용해서 사용하는 방법 (non-static)
//5. 객체의 개성(나이, 이름, 거주지)을 표현할 때는 non-static > 객체마다 다르기 때문에
//6. 모든 객체가 동일하게 사용하는 공유 정보(인류 평균 나이)는 static
//* 일반적으로는 non-static 을 사용하면 된다
//* static이 붙은 것은 static 이 붙지 않은 것을 사용할 수 없다